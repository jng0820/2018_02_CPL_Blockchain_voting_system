<!DOCTYPE HTML>
<% include ../header %>
<% include ./sub_img_vote %>

<html>
    <head>  </head>
    <body>
	<article>
		<h1>[Result]</h1>
        <div id=result>
            
            <br /><br />
            <% var _list = JSON.stringify(candidate)
        _list = JSON.parse(_list)
        %> 
        </br></br>
        <table border="1" bordercolor="skyblue" width="500" height="60" align="center">
                <td>기호</td>
                <td>이름</td>
                <td>정당</td>
                <td>득표수</td>
                <% for(var i=0;i<_list.length;i++){%>
                <tr  align ="center">
                    <td><%=_list[i]["idx"]+1%></td>
                    <td><%=_list[i]["name"]%></td>
                    <td><%=_list[i]["party"]%></td>
                    <td><%=_list[i]["vote_num"]%></td>
                </tr>
                <%}%>
            </table>
            <h1 align="center">기호 <%=_list[0]["idx"]+1%>번 <%=_list[0]["name"]%> 후보자 당선</h1>
        </div>
    </article>
    </body>
</html>

<% include ../footer %>

